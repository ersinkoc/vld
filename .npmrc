# Reproducible installs: use the lockfile exactly.
package-lock=true
save-exact=false

# Don't auto-run lifecycle scripts on `npm install` — the build is committed
# to dist/, and `scripts/verify-install.cjs` audits them in CI.
ignore-scripts=true

# Engine warnings on local install only — CI catches this in verify:install.
engine-strict=false